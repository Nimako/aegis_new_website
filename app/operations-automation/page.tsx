import Link from "next/link";
import { CTA, PageHero } from "../components/site-shell";
import { Card, Icon, SectionHeading } from "../components/ui";

const capabilities = [["lucide:zap", "Event-triggered workflows", "Start the right workflow when a payment posts, a customer replies, a case changes, or a system sends a signal."],["lucide:refresh-cw", "Reconciliation", "Compare payment-rail settlements, operational records, and ledgers so finance teams see the mismatch—not a pile of raw data."],["lucide:activity", "Monitoring", "Watch for unusual activity, workflow drift, missed SLAs, and signals that deserve a closer look."],["lucide:shield-check", "Compliance checks", "Read approved policies and check the relevant facts before a workflow makes a high-impact move."],["lucide:clipboard-check", "Approval gates", "Pause external changes, disbursements, or sensitive outcomes until the right person reviews them."],["lucide:scroll-text", "Execution history", "Keep a durable record of what happened, why it happened, who approved it, and what can be undone."]];
const flow = [["Signal", "A webhook, message, payment, or schedule starts the work."],["Agent analysis", "The right agent gathers context and proposes the next step."],["Policy check", "Rules, sources, confidence, and risk determine what is allowed."],["Action or approval", "Aegis completes the safe action or sends a structured review."],["Monitoring", "The workflow watches the result and catches exceptions."],["Audit trail", "Every decision and handoff stays visible to your team."]];

export default function OperationsAutomationPage(){return <main><PageHero eyebrow="Work that follows the conversation" title={<>Automate the work.<br /><em>Keep the say-so.</em></>}><p>Aegis turns events, policies, and operational context into governed workflows—so your team can move from “what happened?” to “what should happen next?”</p></PageHero><section className="shell page-content"><SectionHeading eyebrow="Agentic operations" title={<>The back office<br /><em>gets a front door.</em></>} copy="Aegis coordinates the repeatable work behind customer and financial journeys, with a clear boundary between action and approval." /><div className="grid-3">{capabilities.map(([icon,title,copy])=><Card key={title}><span className="icon-box"><Icon icon={icon} /></span><h3>{title}</h3><p>{copy}</p></Card>)}</div><div className="section automation-flow"><SectionHeading eyebrow="How a governed workflow moves" title={<>From signal to<br /><em>audit trail.</em></>} /><div className="flow-track">{flow.map(([title,copy], index)=><div className="flow-node" key={title}><span>{String(index+1).padStart(2,"0")}</span><h3>{title}</h3><p>{copy}</p>{index < flow.length-1 && <b className="flow-arrow" aria-hidden="true">→</b>}</div>)}</div></div><div className="automation-grid"><div><span className="eyebrow">A useful default</span><h2 className="section-title">Silent when safe.<br /><em>Guided when it matters.</em></h2></div><div><p className="soft">Choose the autonomy mode per workflow. Let a low-risk task close itself, guide an agent through a decision, or ask for review before anything consequential moves.</p><Link className="button ghost" href="/financial-tooling">See it in financial operations ↗</Link></div></div></section><CTA title={<>Give every workflow<br /><em>a clear next step.</em></>} /></main>}
