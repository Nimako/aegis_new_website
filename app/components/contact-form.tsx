"use client";
import { FormEvent, useState } from "react";
export function ContactForm(){const [sent,setSent]=useState(false);const [error,setError]=useState("");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);if(!form.get("name")||!form.get("email")||!form.get("message")){setError("Please complete your name, email, and message.");return}setError("");setSent(true)}return <form className="card form-stack" onSubmit={submit} noValidate>{sent?<div className="notice success"><strong>Thanks — your request is ready.</strong><br />The contact service is not connected yet, so no message was sent. This demo keeps your information on this page only.</div>:<><div className="field-grid"><div className="field"><label htmlFor="name">Your name <span>REQUIRED</span></label><input id="name" name="name" autoComplete="name" /></div><div className="field"><label htmlFor="email">Work email <span>REQUIRED</span></label><input id="email" name="email" type="email" autoComplete="email" /></div></div><div className="field"><label htmlFor="company">Company</label><input id="company" name="company" autoComplete="organization" /></div><div className="field"><label htmlFor="message">What would you like to build? <span>REQUIRED</span></label><textarea id="message" name="message" rows={6} /></div>{error&&<p className="error" role="alert">{error}</p>}<button className="button" type="submit">Send request ↗</button></>}</form>}
