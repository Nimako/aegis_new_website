import Link from "next/link";
import { CTA, PageHero } from "../components/site-shell";
const resources = [["connectors","Knowledge connectors","Bring approved sources from Drive, SharePoint, OneDrive, Teams, Slack, Notion, and Confluence."],["security","Security & compliance","Understand how Aegis keeps policies, approvals, and execution history visible."],["deployment","Deployment guide","Explore website, WhatsApp, voice, SMS, and API/SDK deployment paths."],["playbooks","Workflow playbooks","See practical starting points for support, booking, follow-up, and review." ]];
export default function ResourcesPage(){return <main><PageHero eyebrow="Resources" title={<>Make the next step <em>clear.</em></>}><p>Guides and practical starting points for building governed customer and team workflows.</p></PageHero><section className="shell page-content">{resources.map(([id,title,copy])=><article className="list-card" id={id} key={id}><div><span className="eyebrow">Aegis resource</span><h3>{title}</h3><p>{copy}</p></div><Link className="button ghost small" href="/contact">Request guide ↗</Link></article>)}</section><CTA /></main>}
