import Link from "next/link";
import { CTA, PageHero } from "../components/site-shell";
import { Card, Icon } from "../components/ui";

const features = [["lucide:inbox", "Shared inbox", "Bring web, WhatsApp, voice, and SMS into one calm workspace."], ["lucide:sparkles", "Assistants", "Answer, qualify, and take the next step with the right amount of autonomy."], ["lucide:shield-check", "Approval queue", "Review important changes before they move forward."], ["lucide:route", "Customer journey", "Keep the full context from the first message to the final outcome."], ["lucide:sliders-horizontal", "Policy controls", "Set boundaries by workflow and make them visible to your team."], ["lucide:scroll-text", "Execution log", "Trace what happened, who approved it, and what can be undone."]];

export default function ProductPage() { return <main><PageHero eyebrow="One calm control plane" title={<>Everything your team needs <em>in one workspace.</em></>}><p>From the first message to the final audit, Aegis keeps people, assistants, and policy in the same room.</p></PageHero><section className="shell page-content" id="workflows"><div className="grid-3">{features.map(([icon, title, copy]) => <Card key={title}><div className="icon-box"><Icon icon={icon} /></div><h3>{title}</h3><p>{copy}</p></Card>)}</div><div className="section" id="channels"><div className="section-heading"><div><span className="eyebrow">Every useful channel</span><h2 className="section-title">Meet people <em>where they are.</em></h2></div><p>Carry the same helpful context into the tools your team already uses.</p></div><div className="grid-3">{[["lucide:message-circle","Website chat"],["simple-icons:whatsapp","WhatsApp"],["lucide:phone-call","Voice calls"],["lucide:message-square","SMS"],["lucide:braces","API / SDK"],["lucide:book-open-check","Knowledge connectors"]].map(([icon,item], i) => <Card key={item}><span className="icon-box"><Icon icon={icon} /></span><h3>{item}</h3><p>{i < 3 ? "Connected and ready for your workflow." : "Ready to connect when your team is."}</p></Card>)}</div></div><div className="section band" id="governance"><span className="eyebrow">Stay in charge</span><h2 className="section-title">Let assistants do the work.<br /><em>Keep control of the outcome.</em></h2><p className="soft governance-copy">Choose silent, guided, or ask mode per workflow. See every step, review important changes, and undo when needed.</p><Link className="button ghost" href="/get-started">Choose your starting point ↗</Link></div></section><CTA /></main>; }
