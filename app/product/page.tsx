import Link from "next/link";
import { CTA, PageHero } from "../components/site-shell";

const features = [["▣", "Shared inbox", "Bring web, WhatsApp, voice, and SMS into one calm workspace."], ["✦", "Assistants", "Answer, qualify, and take the next step with the right amount of autonomy."], ["✓", "Approval queue", "Review important changes before they move forward."], ["◌", "Customer journey", "Keep the full context from the first message to the final outcome."], ["⌁", "Policy controls", "Set boundaries by workflow and make them visible to your team."], ["▤", "Execution log", "Trace what happened, who approved it, and what can be undone."]];

export default function ProductPage() { return <main><PageHero eyebrow="One calm control plane" title={<>Everything your team needs <em>in one workspace.</em></>}><p>From the first message to the final audit, Aegis keeps people, assistants, and policy in the same room.</p></PageHero><section className="shell page-content" id="workflows"><div className="grid-3">{features.map(([icon, title, copy]) => <article className="card" key={title}><div className="icon-box">{icon}</div><h3>{title}</h3><p>{copy}</p></article>)}</div><div className="section" id="channels"><div className="section-heading"><div><span className="eyebrow">Every useful channel</span><h2 className="section-title">Meet people <em>where they are.</em></h2></div><p>Carry the same helpful context into the tools your team already uses.</p></div><div className="grid-3">{["Website chat", "WhatsApp", "Voice calls", "SMS", "API / SDK", "Knowledge connectors"].map((item, i) => <div className="card" key={item}><span className="icon-box">{["⌁", "◌", "◉", "□", "{}", "▤"][i]}</span><h3>{item}</h3><p>{i < 3 ? "Connected and ready for your workflow." : "Ready to connect when your team is."}</p></div>)}</div></div><div className="section band" id="governance"><span className="eyebrow">Stay in charge</span><h2 className="section-title">Let assistants do the work.<br /><em>Keep control of the outcome.</em></h2><p className="soft" style={{ maxWidth: 500 }}>Choose silent, guided, or ask mode per workflow. See every step, review important changes, and undo when needed.</p><Link className="button ghost" href="/get-started">Choose your starting point ↗</Link></div></section><CTA /></main>; }
